import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.template',
  appName: 'MyTemplate',
  // NOTE: For Capacitor, you typically build the web app and copy the output to a static folder.
  // We are not wiring the full workflow yet—this is a placeholder config.
  webDir: 'out',
};

export default config;
